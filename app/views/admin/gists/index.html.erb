<div class="row justify-content-center mt-3">
  <div class="col col-md-4"><h1><%= t('.all_gists') %></h1>

    <div class="col-12">
      <table class="table table-bordered border-primary">
        <tr>
          <th><%= t('.question') %></th>
          <th><%= t('.url') %></th>
          <th><%= t('.user') %></th>
        </tr>

      <%= render @gists %>

    </table>
  </div>
</div>

