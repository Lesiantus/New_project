
<div class="row justify-content-center mt-3">
  <div class="col col-md-6">
    <h2><%= t('.title') %>
    </h2>
    <p><%= @badge.title %></p>
      <%= image_tag("#{@badge.image_name}", width:'200px') %>
    </p>
    <p><%= t('.rule') %></p>
    <p><%= @badge.rule %></p>

    <p>
      <%= link_to t('.edit') , edit_admin_badge_path(@badge) %>
      <%= link_to t('.new') , new_admin_badge_path(@badge) %>
      <%= link_to t('.back') , admin_badges_path %>
    </p>
  </div>
</div>
