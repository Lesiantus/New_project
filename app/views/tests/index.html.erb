<div class="row justify-content-center mt-3">
  <div class="col col-md-4"><h1><%= t('tests.index.header') %></h1>

    <div class="col-12">
      <table class="table table-bordered border-primary">
        <tr>
          <th><%= t('.title') %></th>
          <th><%= t('.level') %></th>
          <th></th>
        </tr>

        <%= render @tests %>
      </table>
    </div>
  </div>
</div>

