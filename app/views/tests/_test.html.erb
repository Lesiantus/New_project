<tr>
  <td><%= test.title %></td>
  <td><%= test_level(test) %></td>
  <td><%= test.timer ? t('.time_limit', timer: test.timer) : t('.time_unlimited') %></td>
  <td><%= button_to t('.start'), start_test_path(test), class: 'btn btn-primary' %></td>
</tr>

